<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Form</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
        }

        .login-form {
            max-width: 400px;
            margin: 50px auto;
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }

        .form-group {
            margin-bottom: 15px;
        }

            .form-group label {
                display: block;
                font-weight: bold;
            }

            .form-group input[type="text"],
            .form-group input[type="password"] {
                width: calc(100% - 20px);
                padding: 8px 10px;
                font-size: 16px;
                border: 1px solid #ccc;
                border-radius: 4px;
            }

            .form-group input[type="checkbox"] {
                margin-right: 10px;
            }

            .form-group .forgot-password {
                font-size: 14px;
                text-decoration: underline;
                cursor: pointer;
            }

            .form-group .btn-container {
                margin-top: 20px;
            }

                .form-group .btn-container button {
                    padding: 10px 20px;
                    background-color: #4CAF50;
                    color: white;
                    border: none;
                    border-radius: 4px;
                    cursor: pointer;
                }

                    .form-group .btn-container button.reset-btn {
                        background-color: #f44336;
                        margin-left: 10px;
                    }
    </style>
</head>
<body>

    <div class="login-form">
        <form action="#" method="post">
            <div class="form-group">
                <label for="email">Email</label>
                <input type="text" id="email" name="email" required>
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" required>
            </div>
            <div class="form-group">
                <label>
                    <input type="checkbox" id="keep-logged" name="keep-logged">
                    Keep me logged in
                </label>
            </div>
            <div class="form-group">
                <a class="forgot-password" onclick="alert('Forgot password link clicked')">Forgot password?</a>
            </div>
            <div class="form-group">
                <label>
                    <input type="checkbox" id="first-login" name="first-login">
                    First login?
                </label>
            </div>
            <div class="form-group btn-container">
                <button type="submit">OK</button>
                <button type="reset" class="reset-btn">Reset</button>
            </div>
        </form>
    </div>

</body>
</html>
